// Re-export shim: รวม path การ import toast ให้ใช้ "@/hooks/use-toast" ได้
// (ตัว implementation จริงอยู่ที่ "@/components/ui/use-toast")
export { useToast, toast } from "@/components/ui/use-toast";
